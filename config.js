export const apiKey = undefined;
export const categories = ['latest', 'now_playing', 'popular', 'top_rated', 'upcoming'];
export const checkForTrailerOnClick = false;

export default {
  categories,
  apiKey
}
